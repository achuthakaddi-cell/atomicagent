{
    "name": "atomicagent",
    "version": "0.1.0",
    "private": true,
    "packageManager": "pnpm@11.21.0",
    "description": "AI sourcing agent for MSMEs. Three x402-gated checks, one Algorand atomic settlement.",
    "type": "module",
    "engines": {
      "node": ">=22.13.0"
    },
    "scripts": {
      "dev": "concurrently --names \"PRICE,AVAIL,VERIFY,ORCH,WEB\" --prefix-colors \"cyan,magenta,yellow,green,blue\" --kill-others \"pnpm --filter @atomicagent/service-price dev\" \"pnpm --filter @atomicagent/service-availability dev\" \"pnpm --filter @atomicagent/service-verification dev\" \"pnpm --filter @atomicagent/orchestrator dev\" \"pnpm --filter @atomicagent/web dev\"",
      "typecheck": "pnpm -r --parallel typecheck",
      "build": "pnpm -r build",
      "verify": "node scripts/verify-structure.mjs",
      "health": "node scripts/health-check.mjs"
    },
    "devDependencies": {
      "@types/node": "^22.10.2",
      "algosdk": "^3.6.0",
      "concurrently": "^9.1.2",
      "tsx": "^4.19.2",
      "typescript": "^5.7.2"
    }
  }